---
export interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span 
  class="text-sm inline-flex items-center justify-center px-6 py-1 rounded-full border border-accent bg-transparent text-accent bg-accent/10" 
  role="status"
  aria-label={label}
>
  {label}
</span>